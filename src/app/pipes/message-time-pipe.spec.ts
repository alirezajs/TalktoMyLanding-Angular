import { MessageTimePipe } from './message-time-pipe';

describe('MessageTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new MessageTimePipe();
    expect(pipe).toBeTruthy();
  });
});
